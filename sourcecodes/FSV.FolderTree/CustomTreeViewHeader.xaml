<UserControl x:Class="FSV.FolderTree.CustomTreeViewHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid Height="20">
        <StackPanel Orientation="Horizontal">
            <Image x:Name="Icon" Height="20" Margin="0,0,0,0" Source="{Binding Image, Mode=OneWay}" Width="20" />
            <TextBlock Margin="5,0,0,0" TextAlignment="Center" VerticalAlignment="Center" x:Name="Text"
                       Text="HostA: Disconnected" />
        </StackPanel>
    </Grid>
</UserControl>