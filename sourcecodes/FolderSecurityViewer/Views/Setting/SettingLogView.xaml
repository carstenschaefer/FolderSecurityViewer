<UserControl x:Class="FolderSecurityViewer.Views.Setting.SettingLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:res="clr-namespace:FSV.Resources;assembly=FSV.Resources"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border>
        <Button Content="{x:Static res:ConfigurationResource.OpenLogFolderCaption}" HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Command="{Binding OpenLogCommand}" />
    </Border>
</UserControl>