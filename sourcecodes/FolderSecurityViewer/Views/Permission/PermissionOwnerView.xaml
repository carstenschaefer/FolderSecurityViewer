<UserControl x:Class="FolderSecurityViewer.Views.Permission.PermissionOwnerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:FolderSecurityViewer.Controls"
             xmlns:res="clr-namespace:FSV.Resources;assembly=FSV.Resources"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <controls:TextLabel Padding="5" FontWeight="Bold">
        <Run Text="{x:Static res:PermissionResource.OwnerCaption}" />
        :
        <Run Text="{Binding OwnerName, Mode=OneWay}" />
    </controls:TextLabel>
</UserControl>